import { TextField } from "@mui/material";
import { styled } from '@mui/system';

export const Input = styled(TextField)({
   margin: 3,
   backgroundColor: 'red',
   padding: 8,
   borderRadius: 4,
});
